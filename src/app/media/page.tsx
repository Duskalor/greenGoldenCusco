import type { Metadata } from 'next'
import { MediaClient } from '@/components/pages/MediaClient'
export const metadata: Metadata = { title: 'Media | Green Golden Cusco' }
export default function MediaPage() { return <MediaClient /> }
